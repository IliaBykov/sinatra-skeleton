<h1>Home Page</h1>

<section id="pin-container">
    <% @pins.each do |pin| %>
        <div class="pin">
            <h2><%= pin.title %></h2>
        </div>
    <% end %>
</section>

<a href="/pins/new" class="btn btn-success">Add a Pin</a>